#!/usr/bin/env bash

if [[ $# -eq 1 ]]; then
    selected=$1
else
    selected=$(find ~/ -mindepth 1 -maxdepth 5 \( \( -type f -name "*.code-workspace" \) -o \( -type d -name '.git' \) \) 2> /dev/null | sed 's/.git//g' | fzf)
fi

if [[ -z $selected ]]; then
    exit 0
fi

code $selected
